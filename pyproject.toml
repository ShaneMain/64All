[tool.poetry]
name = "your_project_name"
version = "0.1.0"
description = "Your project description"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
gitpython = "^3.1.30"  # As of the latest version
ttkthemes = "^3.2.2"  # As of the latest version
Pillow = "^10.0.1"  # Tkinter often needs Pillow for image supp ort
darkdetect = "0.8.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"